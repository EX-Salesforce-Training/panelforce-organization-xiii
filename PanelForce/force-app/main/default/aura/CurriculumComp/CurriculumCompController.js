({
    
    addTopicHandler: function(component,event,helper){

        let message = event.getParam("addTopics");
        // We store the value from the event inside the attribute of Bob.
        //component.set("v.Bob", message);
